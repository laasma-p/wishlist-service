<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wishlists</title>
  </head>
  <body>
    <h1>Hello from wishlists!</h1>
    <ul>
      <% wishlists.forEach(wishlist => { %>
      <li>
        <a href="/wishlist/<%= wishlist.id %>"> <%= wishlist.title %> </a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
